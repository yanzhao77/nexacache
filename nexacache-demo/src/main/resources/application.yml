spring:
  application:
    name: nexacache-demo
  datasource:
    url: jdbc:sqlite:${user.home}/nexacache_demo.db
    driver-class-name: org.sqlite.JDBC

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

nexacache:
  enabled: true
  scan-packages:
    - io.nexacache.demo.entity

logging:
  level:
    io.nexacache: DEBUG
